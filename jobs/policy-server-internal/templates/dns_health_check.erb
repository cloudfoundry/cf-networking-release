<% unless p("disable") %>
<%
  def ip
    p("listen_ip")
  end

  def port
    p("health_check_port")
  end
%>

curl <%= ip %>:<%= port %>/health \
  --connect-timeout <%= p("connect_timeout_seconds") %>
<% end %>
