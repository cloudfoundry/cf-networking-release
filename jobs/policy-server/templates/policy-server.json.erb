<%=
    require 'json'

    config = p("policy-server")

    # exclude these properties
    excludes = ["ca_cert", "server_cert", "server_key"]
    excludes.each { |k| config.delete k }

    JSON.pretty_generate(config)
%>
