<% if p("release_level_backup") %>
<%=
    require 'json'
    toRender = {
      "username" => link('cfnetworkingdbconn').p('database.username'),
      "password" => link('cfnetworkingdbconn').p('database.password'),
      "host" => link('cfnetworkingdbconn').p('database.host'),
      "port" => link('cfnetworkingdbconn').p('database.port'),
      "database" => link('cfnetworkingdbconn').p('database.name'),
      "adapter" => link('cfnetworkingdbconn').p('database.type'),
    }

    JSON.pretty_generate(toRender)
%>
<% end %>
