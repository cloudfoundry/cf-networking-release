<%=
    require 'json'

    toRender = {
      "api" => "api." + p("system_domain"),
      "apps_domain" => p("apps_domain"),
      "skip_ssl_validation" => p("skip_ssl_validation"),
      "admin_user" => p("admin_user"),
      "admin_password" => p("admin_password"),
      "admin_client" => p("admin_client"),
      "admin_secret" => p("admin_secret"),
      "default_security_groups" => p("default_security_groups"),
      "proxy_applications" => p("proxy_applications"),
      "proxy_instances" => p("proxy_instances"),
      "test_applications" => p("num_apps"),
      "test_app_instances" => p("num_app_instances"),
      "extra_listen_ports" => 2,
      "prefix" => p("prefix"),
    }

    JSON.pretty_generate(toRender)
%>
