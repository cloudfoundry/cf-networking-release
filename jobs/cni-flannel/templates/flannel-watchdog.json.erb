<%=
    require 'json'

    toRender = {
      "bridge_name" => "cni-flannel0",
      "flannel_subnet_file" => "/var/vcap/data/flannel/subnet.env",
      "metron_address" => p("cf_networking.flannel_watchdog.metron_address"),
      "health_check_port" => p("cf_networking.health_check_port"),
      "no_bridge" => p("cf_networking.flannel_watchdog.no_bridge"),
      "metadata_filename" => "/var/vcap/data/container-metadata/store.json",
    }

    JSON.pretty_generate(toRender)
%>
