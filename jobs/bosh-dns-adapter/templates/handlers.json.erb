<% if p('cf_app_sd_disable') %>
<%=

  config = []

require 'json'
JSON.dump(config)
%>
<% else %>
 <%=

config = p('internal_domains').map{
  |domain| {
    'domain' => domain,
    'cache' => {'enabled' => false},
    'source' => {
      'type' => 'http',
      'url' => 'http://127.0.0.1:8053'
    }
  }
}

require 'json'
JSON.dump(config)
%>

<% end %>
