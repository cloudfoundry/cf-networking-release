---
applications:
  - name: smoke
    memory: 32M
    disk_quota: 64M
    buildpacks: [go_buildpack]
    env:
      GOPACKAGENAME: smoke
      GOVERSION: latest
      PROXY_APP_URL: http://proxy.some-cf-deployment.example.com
