---
image_resource:
  type: docker-image
  source: { repository: c2cnetworking/deploy }

platform: linux

inputs:
  - name: version
  - name: cf-networking-ci
  - name: cf-networking-deployments
  - name: cf-networking-tarball
  - name: cf-networking-master

outputs:
  - name: final-release
  - name: release

run:
  path: cf-networking-ci/ci/tasks/shipit
