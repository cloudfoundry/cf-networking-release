---
properties:
  <<: (( merge ))
  uaa:
    <<: (( merge ))
    scim:
      <<: (( merge ))
      users:
      - <<: (( merge ))
      - network-admin|network-admin|openid,network.admin,cloud_controller.admin
<<: (( merge ))
