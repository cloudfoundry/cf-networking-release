---
properties:
  <<: (( merge ))
  uaa:
    <<: (( merge ))
    scim:
      <<: (( merge ))
      users:
      - admin|password|scim.write,scim.read,openid,cloud_controller.admin,doppler.firehose,routing.router_groups.read,network.admin

<<: (( merge ))
